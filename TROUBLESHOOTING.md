# 오늘의 하늘 — 트러블슈팅 및 설계 고려사항

목업에서 실제 서비스로 확장하며 만난 문제를 `증상 → 원인 → 해결 → 고려사항` 중심으로 정리했습니다.

## 1. Vue 기본 기능

### 상세보기 버튼과 카드 클릭이 함께 실행됨

- **증상:** 카드 안의 상세보기 버튼을 누르면 버튼과 카드의 동작이 모두 실행됐습니다.
- **원인:** 자식의 클릭 이벤트가 부모로 전달되는 이벤트 버블링 때문입니다.
- **해결:** `@click.stop`으로 전파를 막았습니다. 이후 카드 내부 즐겨찾기 버튼에도 같은 원칙을 적용했습니다.

```vue
<button @click.stop="showDetails(weather)">상세 보기</button>
```

### 검색 문자열 대신 DOM 요소가 저장됨

`searchQuery = e.target`으로 작성해 `includes()` 검색이 실패했습니다. `e.target`은 input 요소이고 실제 문자열은 `e.target.value`입니다. 한글 IME 입력까지 직접 처리하는 복잡성을 줄이기 위해 이후에는 PrimeVue AutoComplete의 모델 갱신 이벤트를 사용했습니다.

### watcher가 실행되지 않음

`selectedCityInfo`와 watcher를 선언했지만 카드 함수에서 값을 바꾸지 않았습니다. watcher는 선언만으로 실행되지 않으므로 실제 사용자 동작에서 `selectedCityInfo.value = city`를 연결했습니다. 템플릿의 `showDetails()` 함수 자체가 script에 빠졌던 문제도 함께 확인했습니다.

### 사소하지만 빌드가 잡지 못한 문제

- `placeholder`를 `placehoder`로 써 안내 문구가 나오지 않았습니다.
- `ºC` 대신 정확한 `°C` 또는 `℃`를 사용했습니다.
- wrapper가 늘어나 `.cards > div` CSS가 실제 카드가 아닌 부모에 적용됐습니다. 최종적으로 카드에 명시적인 class를 두고 scoped style에서 처리했습니다.
- 목록의 `key`는 index가 아니라 도시 ID를 사용했습니다. 즐겨찾기 정렬 후에도 DOM 상태를 안정적으로 유지하기 위해서입니다.

## 2. 컴포넌트 분리

### props와 emits 연결 실패

자식의 emit 이름과 부모 listener가 다르면 이벤트가 전달되지 않습니다. `update-query`, `select-card`, `click-detail`, `add-city`처럼 kebab-case로 통일하고 `defineEmits`, `emit()`, 부모 listener 세 곳을 함께 확인했습니다.

props는 읽기 전용이므로 SearchBar가 직접 수정하지 않고 새 값을 emit하며, 부모가 실제 상태를 변경하도록 했습니다.

### Vue 파일에 블록이 중복됨

기존 함수 아래에 새 코드를 복붙하면서 `<script setup>`과 `<template>`이 두 개씩 생겼습니다. 기존 함수를 무조건 버리지 않고 새 코드와 비교해 각 블록을 하나로 병합했습니다.

### slot의 위치와 데이터 scope 혼동

`<slot></slot>`의 내용은 BaseDashboardCard 내부에 보이지만 상태와 이벤트는 그 내용을 작성한 부모 scope를 사용합니다. 공통 카드는 레이아웃만 담당하고 검색 상태는 부모가 소유하도록 구분했습니다.

### App.vue가 너무 커짐

일출·일몰, 테마 계산, 추천 문구, 헤더 UI가 App에 모이자 책임이 불분명해졌습니다. 보이는 공통 UI는 `WeatherHeader.vue`, 시간과 예보를 이용한 반응형 계산은 `useWeatherTheme.js`로 분리했습니다. 리팩토링은 기능 변경이 아니므로 기존 계산 기준과 화면 결과를 유지한 채 lint와 build로 회귀를 확인했습니다.

## 3. Router

- 이동에는 `useRouter()`, 현재 주소의 `params`를 읽을 때는 `useRoute()`를 사용했습니다.
- 잘못된 도시 ID에서 `undefined` 속성을 읽지 않도록 라우트 가드를 두고, catch-all route는 가장 마지막에 배치했습니다.
- Vercel에서 `/weather/:id`를 직접 새로고침하면 실제 파일을 찾다가 404가 될 수 있어 `vercel.json`에서 모든 요청을 `index.html`로 rewrite했습니다.

## 4. Pinia와 온도 변환

### Store가 동작하지 않은 오타

`useCongigStore`/`useConfigStore`, `fahrenhit`/`fahrenheit`가 섞였고 `useConfigStore` 뒤의 호출 괄호를 빠뜨린 적도 있었습니다. 공유 상태의 이름은 여러 화면에 영향을 주므로 철자와 `useConfigStore()` 호출 여부를 확인했습니다.

### 변환 로직 중복과 오차 누적

WeatherCard와 DetailView에 화씨 계산을 복사하지 않고 `useTemperature` composable로 분리했습니다. 원본은 항상 섭씨로 두고 computed에서 표시값만 변환합니다. 원본을 변환값으로 덮으면 단위를 반복 전환할 때 반올림 오차가 누적될 수 있습니다. `toValue()`로 ref, getter, 일반 값을 모두 받을 수 있게 했습니다.

## 5. OpenWeather

### API 응답과 기존 카드 구조가 다름

OpenWeather는 `main.temp`, `weather[0]`처럼 중첩된 값을 반환합니다. UI 전체가 외부 API 형식에 종속되지 않도록 API 파일에서 `{ id, name, temp, status, humidity, wind, lat, lon }` 형태로 정규화했습니다.

### 지역명이 영어이고 `온흐림`이 표시됨

`lang=kr`은 좌표 기반 지역명까지 항상 번역하지 않습니다. 기본 도시는 요청에 사용한 한국어 이름을, 검색 도시는 Geocoding의 `local_names.ko`를 우선했습니다. 어색한 description 대신 `Clear → 맑음`, `Clouds → 흐림`처럼 `weather.main`을 서비스 문구로 매핑했습니다.

### API 추가 후 기존 검색이 사라짐

새 요청 로직으로 기존 computed 검색을 대체한 것이 원인이었습니다. 다음 흐름으로 복구했습니다.

```text
화면 진입 → 기본 5지역 요청
입력 중 → 이미 받은 배열을 computed로 필터링
검색 확정 → 새로운 도시만 Geocoding 및 날씨 요청
```

전국 도시를 시작할 때 모두 요청하면 낭비가 크고 한 도시만 받으면 기본 목록이 부족합니다. 기본 5지역을 `Promise.all()`로 받고 나머지는 사용자가 확정한 경우에만 요청하는 절충안을 택했습니다. 로딩, 검색 결과 없음, 인증 실패도 서로 다른 상태로 표시했습니다.

## 6. PrimeVue

버전을 지정하지 않고 설치하자 PrimeVue 5의 `Invalid PrimeUI License` 배너가 나타났습니다. 실습의 라이선스와 호환성을 검토해 아래 MIT 버전으로 고정했습니다.

```sh
npm install primevue@4.5.5 @primeuix/themes@1.2.5 primeicons@7.0.0
```

최신 버전이 항상 적합한 것은 아니며 문서, 테마, 라이선스를 함께 확인해야 합니다. UI 컴포넌트를 교체할 때는 기존 props/emits, Store action, Router 함수를 유지해 표현 계층 변경이 데이터 흐름 변경으로 번지지 않게 했습니다.

## 7. 주간 예보와 UI

Open-Meteo의 날짜·기온·강수 데이터는 각각 배열로 오므로 같은 index를 날짜별 객체 하나로 묶어 API 계층에서 정규화했습니다. 선택 도시 watcher에 `{ immediate: true }`를 주어 첫 진입과 도시 변경 시 예보를 불러왔습니다.

같은 도시를 다시 선택할 때마다 요청하지 않도록 Pinia에 실행 중 데이터만 보관했습니다. 아직 배우지 않은 만료 기반 캐싱이나 localStorage 예보 캐시는 적용하지 않았습니다. 새로고침하면 최신 값을 다시 받는 학습 범위의 절충안입니다.

강수확률 50% 이상을 warning의 빨강·주황으로 표시했지만 비는 시스템 오류가 아닙니다. 0%의 흰색에서 100%의 진한 파랑으로 연결하고, 색상만으로 의미를 전달하지 않도록 물방울, 퍼센트, 게이지 길이를 함께 사용했습니다. 큰 장식 배너는 실제 예보를 아래로 밀어 제거했습니다.

## 8. 일출·일몰과 동적 테마

- 요청의 `daily`, API 정규화, Store, props 중 한 단계라도 sunrise/sunset이 빠지면 표시되지 않으므로 전체 전달 경로를 확인했습니다.
- Open-Meteo에 `Asia/Seoul`을 지정하고 오늘 날짜 계산도 같은 시간대를 사용했습니다.
- computed 안의 `new Date()`는 스스로 반응형이 아닙니다. `currentTime` ref를 1분마다 갱신하고 unmount에서 interval을 제거했습니다.
- 노을·밤·비·낮 조건이 겹칠 수 있어 판정 우선순위를 정하고, 데이터가 오기 전에는 기본 하늘색을 사용했습니다.
- 추천 문구와 제목이 왼쪽에 몰려 일출·일몰을 오른쪽으로 옮겼고 모바일에서는 세로 배치했습니다.

## 9. 사용자 도시와 즐겨찾기

### Pinia 상태가 새로고침 후 사라짐

Pinia는 실행 중 공유 상태이므로 도시 이름·좌표와 즐겨찾기 순서만 localStorage에 저장했습니다. 기온과 습도까지 저장하면 오래된 값이 보이므로 앱 시작 시 좌표를 복원한 뒤 날씨를 다시 요청합니다. JSON이 손상돼도 앱이 멈추지 않도록 `JSON.parse()`를 `try-catch`로 감쌌습니다.

### 도시 중복과 예보 목록 누락

기본 배열과 저장 배열을 단순 결합해 같은 도시가 두 번 나타났습니다. 날씨 목록은 OpenWeather 도시 ID를 이용한 `Map`으로 중복 제거했습니다. 예보 화면도 고정 배열만 쓰지 않고 Store의 추가 도시를 합치도록 변경했습니다. 판교가 선택 목록에 두 번 나타난 문제 역시 서로 다른 객체 참조가 아니라 ID·이름·좌표 같은 안정적인 식별값으로 제거했습니다.

### 즐겨찾기 정렬 의도

“가장 먼저 즐겨찾기한 도시가 맨 위”가 요구사항이므로 배열 삽입 순서를 유지하고 각 이름의 index로 정렬했습니다. 즐겨찾기가 아닌 도시는 뒤로 보냈습니다. 별 버튼에는 `.stop`을 적용해 카드 상세 이동이 함께 실행되지 않게 했습니다.

## 10. 에어코리아

### API 선택과 504

전국 고농도 예보나 경보보다 상세 화면의 현재 PM10·PM2.5에 맞는 실시간 측정정보를 선택했습니다. 처음 사용한 측정소별 API가 반복적으로 504를 반환해 시·도별 실시간 API로 목록을 받은 뒤 측정소를 고르는 방식으로 변경했습니다. 504는 upstream 지연일 수 있으므로 한 번만 재시도하고 무한 재시도는 하지 않았습니다.

### 검색 도시 연결

검색 도시는 측정소명이 없으므로 Geocoding 응답의 `state`를 에어코리아가 요구하는 `서울`, `경기`, `대전` 등의 이름으로 매핑했습니다.

- 기본 5지역: 검증한 시·도와 측정소 사용
- 검색 도시: 해당 시·도에서 PM 값이 있는 측정소를 자동 선택

이 방식은 같은 시·도 안의 유효 측정소이지 반드시 가장 가까운 측정소는 아닙니다. 정확도를 높이려면 측정소 좌표와 거리 계산 API가 더 필요합니다. 대구·대전이 실패했던 것은 시·도 매핑 누락과 과거 localStorage 데이터에 새 필드가 없었던 영향도 있었습니다.

### `-` 값과 인증키

API의 `-`는 농도 0이 아니라 측정 자료 없음이므로 `null`로 정규화하고 사용자에게 설명했습니다. 공공데이터포털의 인코딩 키는 `decodeURIComponent()`로 정리한 뒤 Axios params에 전달해 이중 인코딩 가능성을 줄였습니다.

## 11. 환경변수와 Vercel

`.env`는 Git에서 제외하고 `.env.example`에는 변수명만 남겼습니다. 다만 `VITE_` 변수는 브라우저 번들에 포함되므로 저장소 유출만 막을 뿐 진짜 비밀은 아닙니다. 운영 서비스에서는 서버리스 프록시가 필요합니다.

로컬에서는 되지만 Vercel에서 401이 난 이유는 로컬 `.env`가 배포 환경에 자동 전달되지 않았기 때문입니다. 같은 이름과 실제 값을 Vercel Production/Preview에 등록하고 새로 빌드해야 합니다. Vercel은 public prefix를 Secret으로 저장하려 할 때 경고하므로 현재 클라이언트 구조에서는 Config로 등록했습니다.

### Redeploy 후 옛 제목이 나온 문제

`Redeploy`는 최신 main을 가져오는 것이 아니라 선택한 과거 Deployment의 커밋을 다시 배포합니다. 예전 `hosting add` 항목을 재배포해 최신 `서비스 명 수정` 배포가 Production에서 밀렸습니다. 최신 커밋 Deployment를 Production으로 Promote해 해결했습니다.

배포 기록의 누적은 정상이며 롤백에 사용됩니다. 화면이 여전히 이전 버전이면 커밋 hash, Production 배지, 접속 URL을 먼저 확인하고 그다음 강력 새로고침으로 캐시를 확인합니다.

## 12. 개발 전반의 판단 기준

- 한 컴포넌트만 쓰는 값은 지역 `ref`, 파생값은 `computed`, 여러 화면의 상태는 Pinia, 재사용 계산은 composable에 둡니다.
- localStorage에는 사용자 설정만 저장하고 시간이 지나면 낡는 실시간 데이터는 다시 요청합니다.
- 외부 API 원본은 API 계층에서 화면용 데이터로 바꿉니다.
- 새 기능을 넣기 전에 기존 props/emits와 검색 흐름을 확인해 기능이 사라지지 않게 합니다.
- Axios 상태 코드나 결측 기호를 그대로 노출하지 않고 사용자가 다음 행동을 알 수 있는 문구로 바꿉니다.
- 색상만으로 상태를 전달하지 않고 텍스트, 아이콘, 수치를 함께 제공합니다.
- 변경 후 `npm run lint`와 `npm run build`를 실행하고 주요 클릭·검색·라우팅 흐름도 직접 확인합니다.

## 13. 현재 한계와 개선 방향

- API 키 보호를 위해 Vercel Function 또는 백엔드 프록시가 필요합니다.
- 검색 도시의 대기질 측정소를 거리 기준으로 선택할 수 있습니다.
- 공공 API 지연에 서버 측 timeout, 제한 재시도, 단기 캐시를 적용할 수 있습니다.
- 동명 지역 구분을 위해 Geocoding 후보와 상위 행정구역을 함께 보여줄 수 있습니다.
- localStorage 형식 변경에 대비해 데이터 버전과 migration을 둘 수 있습니다.
- 즐겨찾기, 단위 변환, 라우팅에 컴포넌트 테스트와 E2E 테스트를 추가할 수 있습니다.

[README로 돌아가기](./README.md) · [전체 실습 보고서 보기](./REPORT.md)
